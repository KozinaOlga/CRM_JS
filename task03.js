'use strict';

const totalGoods = 40000; //общая сумма корзины
const quantityItems = 5;     //кол-во товаров в корзине
const promotionalCode = 'METHED';
const promotionalCodeTwo = 'G3H2Z1';

const calculate = (totalGoods, quantityItems, promotionalCode) {
    let amountGoods = totalGoods;
    return
}

if (quantityItems > 10) {
    amountGoods *= 0.97
}